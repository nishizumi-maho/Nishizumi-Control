# Runtime dependencies for running Dominant Control from source on Windows.
keyboard>=0.13.5
pyirsdk>=1.3.6
numpy>=2.0
psutil>=6.0
pywin32>=306

# Overlays: the Qt windows the preserved Nishizumi Tools use.
PyQt5>=5.15.11
PySide6-Essentials>=6.7

# Optional features
pyttsx3>=2.90            # spoken feedback
pyaudio>=0.2.14          # audio device selection
SpeechRecognition>=3.10  # voice triggers
vosk>=0.3.45             # offline voice recognition (needs a downloaded model)
